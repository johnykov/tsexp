export interface User {
    id: bigint
    name: string
    surname: string
    email: string
    phone: string
    address: string
}
//TODO: make email, phone, address value objects with it's own validation
